pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract Target(
    int init_units
) {
    // :: state layouts
    struct ForeignState {
        // :: user declared fields
        int amount;
    }
    struct LocalState {
        // :: user declared fields
        byte[32] foreign_id;
        int steps;
    }
    struct TargetState {
        // :: user declared fields
        int units;
    }

    // :: global functions (isolated using the gen__glob_ namespace)
    function add_one(int gen__glob_value) : int {
        return gen__glob_value + 1;
    }

    // :: actor functions
    function units_are_valid() : bool {
        return units >= 0;
    }

    // :: route templates

    // :: state fields: Target
    int units = init_units;

    // :: entrypoints
    // :: leader entry (1:N)
    entry hold() {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 0);

        require(units_are_valid());
    }

}
