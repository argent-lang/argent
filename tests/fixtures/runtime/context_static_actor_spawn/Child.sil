pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract Child(
    byte[32] gen__init_child_template,
    byte[32] gen__init_launcher_template,
    int init_amount
) {
    // :: state layouts
    struct LauncherState {
        // :: user declared fields
        int launches;
    }
    struct Gen__LauncherState {
        // :: generated fields
        byte[32] gen__child_template;
        byte[32] gen__launcher_template;
        // :: user declared fields
        int launches;
    }

    // :: route templates
    byte[32] gen__child_template = gen__init_child_template;
    byte[32] gen__launcher_template = gen__init_launcher_template;

    // :: state fields: Child
    int amount = init_amount;

    // :: entrypoints
    // :: delegate entry
    entrypoint function support_launch(int gen__launcher_prefix_len, int gen__launcher_suffix_len) {
        // :: cov inputs
        byte[32] gen__cov_id = OpInputCovenantId(this.activeInputIndex);
        require(OpCovInputCount(gen__cov_id) >= 2);
        require(OpCovInputIdx(gen__cov_id, 0) != this.activeInputIndex);
        int gen__launcher_input_idx = OpCovInputIdx(gen__cov_id, 0); // input Launcher at cov[0]
        Gen__LauncherState launcher = readInputStateWithTemplate(
            gen__launcher_input_idx,
            gen__launcher_prefix_len,
            gen__launcher_suffix_len,
            gen__launcher_template
        );

        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 0);

        require(1 == 1);
    }

    // :: leader entry (1:N)
    entrypoint function return_to_launcher(
        byte[] gen__launcher_prefix,
        byte[] gen__launcher_suffix
    ) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        // :: output next: Launcher
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0);

        Gen__LauncherState next = {
            // :: generated fields
            gen__child_template: gen__child_template,
            gen__launcher_template: gen__launcher_template,
            // :: user declared fields
            launches: 0,
        };
        // :: become Launcher
        validateOutputStateWithTemplate(
            gen__next_output_idx,
            next,
            gen__launcher_prefix,
            gen__launcher_suffix,
            gen__launcher_template
        );
    }

}
