pragma silverscript ^0.1.0;

// Generated by argentc. Do not edit by hand.

contract Relay(
    byte[32] gen__init_launcher_template,
    int init_forwards
) {
    // :: state layouts
    struct LauncherState {
        // :: user declared fields
        int launches;
    }
    struct RelayState {
        // :: user declared fields
        int forwards;
    }

    // :: route templates
    byte[32] gen__launcher_template = gen__init_launcher_template;

    // :: state fields: Relay
    int forwards = init_forwards;

    // :: entrypoints
    // :: leader entry (1:N)
    entry forward(int launches, byte[] gen__launcher_prefix, byte[] gen__launcher_suffix) {
        // :: auth outputs
        require(OpAuthOutputCount(this.activeInputIndex) == 1);
        // :: output next: Launcher
        int gen__next_output_idx = OpAuthOutputIdx(this.activeInputIndex, 0);

        LauncherState next_state = LauncherState {
            // :: user declared fields
            launches: launches,
        };
        // :: become Launcher
        validateOutputStateWithTemplate(
            gen__next_output_idx,
            next_state,
            gen__launcher_prefix,
            gen__launcher_suffix,
            gen__launcher_template
        );
    }

}
